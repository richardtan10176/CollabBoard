# CollabBoard Production Environment Configuration
# Copy this file to .env and update with your actual values

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# These settings connect your app to the PostgreSQL database
DB_HOST=postgres
DB_PORT=5432
DB_NAME=collabboard
DB_USER=collabboard

# IMPORTANT: Change this to a secure password!
# Example: MySecureDBPassword2024!
DB_PASSWORD=CHANGE_THIS_SECURE_PASSWORD

# ===========================================
# JWT AUTHENTICATION
# ===========================================
# This secret key is used to sign and verify authentication tokens
# Generate a secure random string using: openssl rand -base64 32
# Example: a8f5f167f44f4964e6c998dee827110c
JWT_SECRET=CHANGE_THIS_TO_A_VERY_SECURE_RANDOM_STRING_AT_LEAST_32_CHARACTERS
JWT_EXPIRES_IN=24h

# ===========================================
# SERVER CONFIGURATION
# ===========================================
PORT=3001
NODE_ENV=production

# ===========================================
# DOMAIN CONFIGURATION
# ===========================================
# Replace "your-domain.com" with your actual domain name
# Example: If your domain is "mycollabboard.com", use:
# FRONTEND_URL=https://mycollabboard.com

# This is where your frontend is accessible
FRONTEND_URL=https://your-domain.com

# ===========================================
# NEXT.JS PUBLIC VARIABLES
# ===========================================
# These are exposed to the browser, so they must be your public domain
# Replace "your-domain.com" with your actual domain name

# API endpoint URL (where the backend API is accessible)
NEXT_PUBLIC_API_URL=https://your-domain.com

# WebSocket URL (for real-time collaboration features)
NEXT_PUBLIC_WS_URL=https://your-domain.com

# ===========================================
# RATE LIMITING
# ===========================================
# These control how many requests users can make
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX_REQUESTS=100  # 100 requests per window

# ===========================================
# EXAMPLE CONFIGURATION
# ===========================================
# Here's what your .env file might look like if your domain is "mycollabboard.com":

# DB_PASSWORD=MySecureDBPassword2024!
# JWT_SECRET=a8f5f167f44f4964e6c998dee827110c
# FRONTEND_URL=https://mycollabboard.com
# NEXT_PUBLIC_API_URL=https://mycollabboard.com
# NEXT_PUBLIC_WS_URL=https://mycollabboard.com
